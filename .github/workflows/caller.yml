name: caller

on: push

jobs:
  call-reusable:
    uses: estudosdevops1/estudo-workflow-templates/.github/workflows/reusable.yml@main
    with:
      nome: 'Igor A B T Santana'
    secrets:
      TOKEN: ${{ secrets.GITHUB_TOKEN }}
